<p style="color: green"><%= notice %></p>
<div>
  <%= link_to "New author", new_author_path, class: "hover:underline text-primary-600 dark:text-primary-500", style: "margin-left: 20px" %>
    <%= link_to 'Gerar Relatório', reports_authors_index_path, class: "hover:underline text-primary-600 dark:text-primary-500", style:"margin-left: 20px"%>
</div>

<div class="max-w-3xl mx-auto px-4 my-16">
  <div class="flex items-center justify-between pb-6 border-b dark:border-zinc-700 border-zinc-200">
    <h3 class="text-3xl font-semibold font-heading text-zinc-900 dark:text-zinc-100 tracking-tight">Author</h3>
  </div>

  <div id="authors">
    <% @authors.each do |author| %>
      <%= render author %>
      <p style="margin-left: 20px">
        <%= link_to "View author", author, class:" hover:underline text-primary-600 dark:text-primary-500 " %>
      </p>
    <% end %>
  </div>
</div>
